// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  getRequestOrigin: 'https://fuqv9yb882.execute-api.ap-south-1.amazonaws.com/dev/',
  postRequestOrigin: 'https://2rx48kl3cf.execute-api.ap-south-1.amazonaws.com/dev/',
  authKey:'598653412845-4a97fdkd74q1gn8b5bq7vitutn34ch2m.apps.googleusercontent.com',
  certificationBaseURI : 'https://y8dnz1n4p5.execute-api.ap-south-1.amazonaws.com/dev/'
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
